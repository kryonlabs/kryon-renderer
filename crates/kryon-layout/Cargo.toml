# crates/kryon-layout/Cargo.toml
[package]
name = "kryon-layout"
version = "0.1.0"
edition = "2021"

[dependencies]
kryon-core = { path = "../kryon-core" }
glam = { workspace = true }
tracing = { workspace = true }
taffy = "0.5"

# Taffy is now the core layout system - no features needed